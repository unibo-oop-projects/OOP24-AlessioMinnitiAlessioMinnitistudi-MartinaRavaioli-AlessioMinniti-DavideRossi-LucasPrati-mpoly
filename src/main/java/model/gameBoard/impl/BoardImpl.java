package model.gameBoard.impl;

import model.Turnation.api.Player;
import model.Turnation.api.Position;
import model.gameBoard.api.Board;
import model.gameBoard.api.Card;
import model.gameBoard.api.Property;

public class BoardImpl implements Board {
    public void sellHouse(Property prop) {}
    public void sellProperty(Property prop) {}
    public Card getCard(Position pos) { return null; }
    public void buyProperty(Property prop, Player owner) {}
}
